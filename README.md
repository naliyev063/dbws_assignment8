# Assignment 8 – Web Log Analysis

This repo contains:
- the script used to parse the Apache logs (`parse_logs.py`)
- the generated CSV files (page_stats, error_timeline, error_types, access_timeline)
- the final analysis report (report.pdf)

To run:
$ python3 parse_logs.py access.log
